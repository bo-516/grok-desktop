# demo workspace rules (for grok agent)

- **Scope**: Only create / edit / delete files under this `demo/` directory.
- **Do not** modify parent repo files (`packages/`, `apps/`, `todo.md`, etc.).
- Prefer small, self-contained examples.
- When asked to use a subagent, spawn a subagent for a bounded subtask and summarize its result in the parent reply.
