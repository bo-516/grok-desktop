/**
 * Demo greeting helper — safe for agent edit experiments.
 */
export function greet(name: string): string {
  return `Hello, ${name}! (demo)`;
}

export function version(): string {
  return "0.1.0";
}
